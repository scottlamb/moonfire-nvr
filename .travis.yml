dist: bionic
addons:
  apt:
    packages:
      - build-essential
      - libavcodec-dev
      - libavformat-dev
      - libavutil-dev
      - libncurses5-dev
      - libncursesw5-dev
      - libsqlite3-dev
      - libssl-dev
      - pkgconf
      - yasm
matrix:
  include:
    - language: rust
      rust: stable
      script:
        - ci/script-rust.sh
    - language: rust
      rust: nightly
      script:
        - ci/script-rust.sh
    - language: rust
      rust: 1.36.0
      script:
        - ci/script-rust.sh
    - language: node_js
      node_js: "node"
      script:
        - yarn
        - yarn build
        - node_modules/eslint/bin/eslint.js ui-src
    - language: node_js
      node_js: "8"
      script:
        - yarn
        - yarn build
        - node_modules/eslint/bin/eslint.js ui-src
  allow_failures:
    - rust: nightly
cache:
  cargo: true
  yarn: true
  timeout: 600
